import React from "react";
import {Shield,Database,RotateCcw} from "lucide-react";
export default function Settings({onReset}){const reset=()=>{if(!window.confirm("Reset all local POS data? This cannot be undone."))return;onReset?.()};return <section className="content"><div className="section-head"><div><h2>Settings</h2><p>Local POS configuration and data controls.</p></div><span className="count">Browser storage</span></div><div className="admin-grid"><article className="admin-item"><div><span>Security</span><strong>Operational controls enabled</strong><small>Staff identity, shifts and audit logging are active.</small></div><Shield size={22}/></article><article className="admin-item"><div><span>Data storage</span><strong>Local browser storage</strong><small>This standalone build keeps operational data in this browser until cloud sync is added.</small></div><Database size={22}/></article><article className="admin-item"><div><span>Danger zone</span><strong>Reset POS data</strong><small>Clears locally stored restaurant records and reloads the demo dataset.</small></div><button className="btn danger-action" onClick={reset}><RotateCcw size={15}/> Reset Data</button></article></div></section>}