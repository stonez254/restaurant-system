import React from "react";
import {ClipboardList,Edit3,Split,ArrowRightLeft} from "lucide-react";
import Empty from "./Empty";
export function OrderCard({order,update,money,onEdit,onSplit,onTransfer}){return <article className="order-card"><div className="order-title"><div><strong>{order.id}</strong><span>{order.channel}{order.table?" • "+order.table:""}{order.customerName?" • "+order.customerName:""} • {new Date(order.createdAt).toLocaleTimeString([], {hour:"2-digit",minute:"2-digit"})}</span></div><span className={"pill "+order.status.toLowerCase().replaceAll(" ","-")}>{order.status}</span></div><div className="order-items">{order.items.map(x=><div key={x.key}><span>{x.qty} × {x.name}</span><small>{x.modifier?.name||x.modifier||"Standard"}</small></div>)}</div><div className="order-foot"><b>{money(order.total)}</b><div className="order-actions">{order.status==="Placed"&&<button onClick={()=>onEdit(order)}><Edit3 size={12}/> Edit</button>}{order.status==="Placed"&&<button onClick={()=>onSplit(order)}><Split size={12}/> Split</button>}{order.channel==="Dine In"&&order.status!=="Served/Out for Delivery"&&<button onClick={()=>onTransfer(order)}><ArrowRightLeft size={12}/> Transfer</button>}{order.status==="Placed"&&<button onClick={()=>update(order.id,"In Prep")}>Start Prep</button>}{order.status==="In Prep"&&<button onClick={()=>update(order.id,"Ready")}>Mark Ready</button>}{order.status==="Ready"&&<button onClick={()=>update(order.id,"Served/Out for Delivery")}>Complete</button>}</div></div></article>}
export default function Orders({orders,update,money,onEdit,onSplit,onTransfer}){return <section className="content"><div className="section-head"><div><h2>Order Queue</h2><p>Edit placed orders, split items and transfer dine-in orders.</p></div><span className="count">{orders.length} total</span></div><div className="order-list">{orders.length?orders.map(o=><OrderCard key={o.id} order={o} update={update} money={money} onEdit={onEdit} onSplit={onSplit} onTransfer={onTransfer}/>):<Empty icon={ClipboardList} title="No orders" text="New POS orders will appear here."/>}</div></section>}