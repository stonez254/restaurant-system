import React,{useState} from "react";
import {Plus,Search} from "lucide-react";
export default function MenuManager({menu,setMenu,categories,money}){const[q,setQ]=useState("");function add(){const name=prompt("Menu item name");if(!name)return;const price=Number(prompt("Price (KSh)"));if(!Number.isFinite(price)||price<=0)return;const category=prompt("Category (Starters, Mains, Sides, Desserts, Drinks)","Mains")||"Mains";setMenu(m=>[...m,{id:Date.now(),name,price,category,active:true}])}const filtered=menu.filter(x=>x.name.toLowerCase().includes(q.toLowerCase()));return <section className="content"><div className="section-head"><div><h2>Menu</h2><p>{menu.length} items across {categories.length} categories.</p></div><button className="primary dark" onClick={add}><Plus size={16}/> Add Item</button></div><div className="toolbar"><div className="search"><Search size={17}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search menu..."/></div></div><div className="admin-grid">{filtered.map(x=><article className="admin-item" key={x.id}><div><span>{x.category}</span><strong>{x.name}</strong></div><b>{money(x.price)}</b></article>)}</div></section>}